export interface MenuItem {
  id: string,
  label: string,
  icon: string,
  iconSelected: string,
}

export interface Message {
  text: string,
  sender: string,
  receiver: string,
  timestamp: string,
}